<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="University.Pages.ViewStudentsHeadPage"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid RowDefinitions="Auto, Auto, Auto, *">
        <TextBlock
            FontSize="20"
            Grid.Row="0"
            HorizontalAlignment="Center"
            Margin="0,10,0,10"
            Text="Список студентов" />

        <StackPanel
            Grid.Row="1"
            Margin="10,0,10,10"
            Orientation="Horizontal">
            <TextBlock Margin="0,0,5,0" VerticalAlignment="Center">Поиск по ФИО:</TextBlock>
            <TextBox
                Height="30"
                TextChanged="SearchBox_TextChanged"
                Width="250"
                x:Name="SearchBox" />
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Center"
            Margin="0,0,0,10"
            Orientation="Horizontal"
            Spacing="15">
            <Button
                Click="AddStudentButton_Click"
                Content="Добавить студента"
                Height="30"
                Width="150" />
        </StackPanel>

        <DataGrid
            Grid.Row="3"
            IsReadOnly="True"
            x:Name="StudentsDataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Binding="{Binding StudentId}"
                    Header="ID"
                    Width="60" />
                <DataGridTextColumn
                    Binding="{Binding RegistrationNumber}"
                    Header="Рег. номер"
                    Width="120" />
                <DataGridTextColumn
                    Binding="{Binding FullName}"
                    Header="ФИО"
                    Width="200" />
                <DataGridTextColumn
                    Binding="{Binding Specialty.Name}"
                    Header="Специальность"
                    Width="180" />
                <DataGridTextColumn
                    Binding="{Binding Login}"
                    Header="Логин"
                    Width="120" />

                <DataGridTemplateColumn Header="Удалить" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="DeleteStudentButton_Click" Content="🗑️" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>