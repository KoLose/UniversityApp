<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="University.Pages.ViewEmployeesEngineerPage"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid RowDefinitions="Auto, Auto, Auto, *">
        <TextBlock
            FontSize="20"
            Grid.Row="0"
            HorizontalAlignment="Center"
            Margin="0,10,0,10"
            Text="Сотрудники" />

        <StackPanel
            Grid.Row="1"
            Margin="10,0,10,10"
            Orientation="Horizontal">
            <TextBlock Margin="0,0,5,0" VerticalAlignment="Center">Поиск по ФИО:</TextBlock>
            <TextBox
                Height="30"
                TextChanged="SearchBox_TextChanged"
                Width="250"
                x:Name="SearchBox" />
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Center"
            Margin="0,0,0,10"
            Orientation="Horizontal"
            Spacing="15">
            <Button
                Click="AddEmployeeButton_Click"
                Content="Добавить сотрудника"
                Height="30"
                Width="160" />
            <Button
                Click="SaveButton_Click"
                Content="Сохранить"
                Height="30"
                Width="100" />
        </StackPanel>

        <DataGrid
            Grid.Row="3"
            IsReadOnly="False"
            x:Name="EmployeesDataGrid">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Binding="{Binding EmployeeId}"
                    Header="Таб. №"
                    IsReadOnly="True"
                    Width="80" />
                <DataGridTextColumn
                    Binding="{Binding FullName}"
                    Header="ФИО"
                    Width="180" />
                <DataGridTextColumn
                    Binding="{Binding Position}"
                    Header="Должность"
                    Width="150" />
                <DataGridTextColumn
                    Binding="{Binding Salary}"
                    Header="Зарплата"
                    Width="100" />
                <DataGridTextColumn
                    Binding="{Binding Chief.FullName}"
                    Header="Шеф"
                    Width="150" />
                <DataGridTextColumn
                    Binding="{Binding Dept.Name}"
                    Header="Кафедра"
                    Width="150" />
                <DataGridTextColumn
                    Binding="{Binding Role.Name}"
                    Header="Роль"
                    Width="100" />
                <DataGridTextColumn
                    Binding="{Binding Login}"
                    Header="Логин"
                    Width="120" />

                <DataGridTemplateColumn Header="Удалить" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="DeleteButton_Click" Content="🗑️" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
